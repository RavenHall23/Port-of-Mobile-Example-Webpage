"use client";
import { useState } from "react";

export default function IndoorWarehouseA() {
  const [selectedSection, setSelectedSection] = useState<string | null>(null);

  return (
    <div className="min-h-screen p-8 flex flex-col items-center justify-center bg-gradient-to-b from-gray-900 to-black">
      <div className="mb-12 text-center">
        <h1 className="text-[32pt] font-[family-name:var(--font-geist-sans)] text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-cyan-300 mb-4">
          Indoor Warehouse A
        </h1>
        <div className="px-8 py-4 rounded-xl bg-gradient-to-r from-gray-800 to-gray-900 shadow-lg border border-gray-700">
          <p className="font-[family-name:var(--font-geist-sans)] text-lg text-gray-300">
            <span className="font-semibold">Warehouse Status: </span>
            <span className="text-emerald-400">Active</span>
            <span className="mx-4">|</span>
            <span className="font-semibold">Capacity: </span>
            <span className="text-blue-400">85%</span>
            <span className="mx-4">|</span>
            <span className="font-semibold">Temperature: </span>
            <span className="text-cyan-400">72°F</span>
          </p>
        </div>
      </div>

      <div className="grid grid-cols-2 gap-8 max-w-4xl w-full">
        <button 
          onClick={() => setSelectedSection('inventory')}
          className="group relative px-8 py-6 rounded-2xl bg-gradient-to-r from-blue-600 to-blue-400 text-white font-[family-name:var(--font-geist-sans)] text-xl shadow-lg hover:shadow-blue-500/50 transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <div className="absolute inset-0 bg-gradient-to-r from-blue-400/20 to-transparent transform -skew-x-12"></div>
          <span className="relative z-10">Inventory Management</span>
        </button>

        <button 
          onClick={() => setSelectedSection('logistics')}
          className="group relative px-8 py-6 rounded-2xl bg-gradient-to-r from-cyan-600 to-cyan-400 text-white font-[family-name:var(--font-geist-sans)] text-xl shadow-lg hover:shadow-cyan-500/50 transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <div className="absolute inset-0 bg-gradient-to-r from-cyan-400/20 to-transparent transform -skew-x-12"></div>
          <span className="relative z-10">Logistics Control</span>
        </button>

        <button 
          onClick={() => setSelectedSection('monitoring')}
          className="group relative px-8 py-6 rounded-2xl bg-gradient-to-r from-teal-600 to-teal-400 text-white font-[family-name:var(--font-geist-sans)] text-xl shadow-lg hover:shadow-teal-500/50 transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <div className="absolute inset-0 bg-gradient-to-r from-teal-400/20 to-transparent transform -skew-x-12"></div>
          <span className="relative z-10">Environmental Monitoring</span>
        </button>

        <button 
          onClick={() => setSelectedSection('security')}
          className="group relative px-8 py-6 rounded-2xl bg-gradient-to-r from-indigo-600 to-indigo-400 text-white font-[family-name:var(--font-geist-sans)] text-xl shadow-lg hover:shadow-indigo-500/50 transition-all duration-300 hover:scale-105 overflow-hidden"
        >
          <div className="absolute inset-0 bg-gradient-to-r from-indigo-400/20 to-transparent transform -skew-x-12"></div>
          <span className="relative z-10">Security Systems</span>
        </button>
      </div>
    </div>
  );
}
