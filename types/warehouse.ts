export type StatusColors = {
  green: { color: string; percentage: string };
  yellow: { color: string; percentage: string };
  orange: { color: string; percentage: string };
  red: { color: string; percentage: string };
}; 